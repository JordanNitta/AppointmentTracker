// The interface in TypeScript is like a blueprint for an object.
// It defines the structure of that object and what is should have and what pproperties it should have and what types those properties should be
export interface Appointment {
    // Creating the interface
    id: number,
    title: string,
    date: Date,
}
